<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-white fixed-top py-3 " id="mainNav">
	<div class="container px-4 px-lg-5 pb-4">
		<a class="navbar-brand text-secondary" href="#page-top">
			<i class="bi bi-arrow-repeat"></i>
			&nbsp FREELANCE CORNER
		</a>
		<div class="dropdown">
			<button class="btn text-secondary btn-md dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
				{{app.user}}
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<a class="dropdown-item" href="{{path('user_profile')}}">Profil</a>
				{% if app.user and 'ROLE_EMP' in app.user.roles %}
				<a class="dropdown-item" href="{{path('user_mission')}}">Missions déposées</a>
				{% endif %}
				<a class="dropdown-item"  href="{{path('mission_list')}}">Liste des missions</a>
				{% if app.user and 'ROLE_ADMIN' in app.user.roles %}
				<a class="dropdown-item" href="{{path('message_list')}}">Message des utilisateurs </a>
				{% endif %}
				<a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion</a>
			</div>
		</div>
	</div>
</nav>
