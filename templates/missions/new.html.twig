{% extends 'base1.html.twig' %}

{% block title %}
    Créer une mission
{% endblock %}

{% block body %}
<section>
    <h1 class="text-primary text-center"> - CREER UNE MISSION - </h1>
    <br><br>
    <div class="row justify-content-center">
        <div class="col-6">
            {{ form_start(form) }}
            {{ form_row(form.title)}}
            {{ form_row(form.description) }}
            {{ form_row(form.price)}}
            {{ form_row(form.deadline)}}
            {{ form_row(form.submit)}}
            {{ form_end(form) }}
        </div>  
    </div>
        
    </div>
</section>
{% endblock %}
